// import { MarkerProperties } from "../../../marker_window/MarkerProperties";
// import { FilterElements, ListElementEntity } from "../../json_element/IFilterElements";

// export class MarkerHelper {

//     static addMarkerToFilterObject(filterElements:FilterElements, marker:ListElementEntity) {
//         // if(NewMarkerModal.getCurrentFilter() === MiscConst.LOCKED_DOOR || ) {    
//         //     marker.y += (filterElements.getFilterElement(MiscConst.LOCKED_DOOR).height / 2);
//         // }
//         filterElements.getListElementEntity(MarkerProperties.getCurrentFilter()).push(marker as ListElementEntity)
//         filterElements.save();
//     }

//     static removeMarkerFromFilterObject(filterElements:FilterElements, iconId:string) {
//         filterElements.removeElementEntity(iconId);
//         filterElements.save();
//     }
// }